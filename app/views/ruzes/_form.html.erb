<%= form_for(@ruze) do |f| %>
  <% if @ruze.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ruze.errors.count, "error") %> prohibited this ruze from being saved:</h2>

      <ul>
      <% @ruze.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :tozihat %><br>
    <%= f.text_area :tozihat %>
  </div>
  <div class="field">
    <%= f.label :tedad %><br>
    <%= f.number_field :tedad %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
